# Telegram bot token from @BotFather
TELEGRAM_BOT_TOKEN=1234567890:ABCdef-replace-me

# Optional: lock owner explicitly (otherwise first user to /start becomes owner)
# OWNER_ID=123456789

# Owner-lock persistence path inside container (default: /data/telegram_owner.json)
# OWNER_FILE=/data/telegram_owner.json

# ─── User's tool env vars (filled in by skill from target .env) ───
{{USER_ENV_VARS}}
